name: build_and_push

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: test
        run: ./gradlew test
#      - name: Build Gem & Put it into './build/gems/' Directory
      - run: ls
      - run: ./gradlew gem
      - run: ls
      - run: dir="$(pwd)"
      - run: cd ${dir}/build/gems
      - run: ls